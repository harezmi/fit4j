tests:
  - name: RestExampleAcceptanceTest
    fixtures:
      - request:
          protocol: http
          path: "/hello"
          response:
            status: 200
            body:
              message: "Hello, Kenan!"
      - request:
          protocol: http
          path: "/bye"
          predicate: "#request.method == 'POST'"
          response:
            status: 200
            body:
              message: "Bye, Umut!"
